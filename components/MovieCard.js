import React from 'react';
import { Text, View } from 'react-native';

const MovieCard = ({}) => {
    return (
        <View>
            <Text>titulo</Text>
        </View>
    );
};

export default MovieCard;