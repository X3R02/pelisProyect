import React from 'react';
import { ScrollView } from 'react-native';

const MainScreen = () => {
    return (
        <ScrollView>
            
        </ScrollView>
    );
};

export default MainScreen;